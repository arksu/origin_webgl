origin {
    db {
        host = localhost
        port = 3306
        user = root
        password = 1
        name = origin
    }

    net {
        port = 8020
    }

    dev {
        mode = true
        assets_dir = "../frontend/assets/"
    }
}